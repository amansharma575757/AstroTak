import 'package:flutter/material.dart';

class AskQuesiontScreen extends StatelessWidget {
  const AskQuesiontScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("ask Quesion"),
    );
  }
}
